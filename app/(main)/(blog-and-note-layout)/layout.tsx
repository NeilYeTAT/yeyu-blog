import BlogAndNoteLayout from '@/ui/(main)/(blog-and-note-layout)/layout'

export default function Layout({ children }: { children: React.ReactNode }) {
  return <BlogAndNoteLayout>{children}</BlogAndNoteLayout>
}
